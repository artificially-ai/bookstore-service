spring:
  application:
    name: playready-service
  cloud:
    config:
      uri: http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5432/playready
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop